{% extends 'base.html.twig' %}

{% block body %}
    <div class="container my-5 text-center">
        <img src="/assets/img/yes.png" alt="Commande confirmée" id="imgpayment">
        <h1 class="thanks_commande">Merci pour votre commande</h1>

        {# {{ dump(order) }} #}
        <a href="{{ path('app_account_order', { 'id_order' : order.id } ) }}" class="btn btn-success">Consulter ma commande</a>
    </div>
{% endblock %}