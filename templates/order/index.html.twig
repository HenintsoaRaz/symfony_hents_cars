<!doctype html>
<html lang="fr" data-bs-theme="auto">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>CARS SHOP</title>
    <meta name="description" content="Vente de voiture">
    <link href="{{ asset('assets/css/custom.css') }}" rel="stylesheet">

    <link href="{{ asset('assets/css/styles.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/css/order.css') }}" rel="stylesheet">


    <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="{{ asset('assets/css/carousel.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/css/custom.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css">

    <script src="https://unpkg.com/feather-icons"></script>
</head>
<body>

<header data-bs-theme="dark">
    <nav class="navbar navbar-expand-md fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}"> <img class="logo_img_brand" src="{{ asset('assets//images/lg.jpg') }}" alt=""> </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav me-auto mb-2 mb-md-0">
                   <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}" id="home-color-m">Accueil</a>
                   </li>
                   {% for category in allCategories %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_category', { slug : category.slug } ) }}">{{ category.name }}</a>
                        </li>

                   {% endfor %}
                </ul>

                <div class="speed-menu">

                    {% if app.user %}
                        <a href="{{ path('app_account') }}"><i data-feather="user"></i> {{ app.user.firstname }} </a> 
                            <a href="{{ path('app_account_wishlist') }}">
                                <i data-feather="heart"></i>
                            </a>
                    {% else %}
                        <a href="{{ path('app_login') }}"><i data-feather="user"></i></a> 
                    {% endif %}

                    <a href="{{ path('app_cart') }}">
                        <i data-feather="shopping-cart"></i>
                        <span class="badge text-bg-light">{{ fullCartQuantity }} </span>
                    </a>
                </div>
            </div>
        </div>
    </nav>
</header>


    <div class="container" id="font-light_gray">
        
        <h1 id="maCommande"> Ma commande </h1>

        {{ form_start(deliverForm) }}
            {{ form_row(deliverForm.addresses) }}
                <a href="{{ path("app_account_address_form") }}" class="small lien_ajout_adresse">Ajouter une nouvelle adresse de livraison </a>

                <hr id="hr">

            {{ form_row(deliverForm.carriers) }}
        {{ form_end(deliverForm) }}
    
	</div>

    
    


<script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}"></script>
<script src="{{ asset('assets/js/global.js') }}"></script>
</body>
</html>